// Importa el componente que contiene la lógica de la vista de gestión
import { ProductTable } from '../features/product/components/productTable'; // Asegúrate de la ruta correcta

export function ProductPage() {
  return (
      <ProductTable/>
  );
}
